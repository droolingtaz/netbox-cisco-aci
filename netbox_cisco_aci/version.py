"""Single source of truth for the plugin version.

Bump this and add a matching `CHANGELOG.md` entry when cutting a release.
The `pyproject.toml` build picks the version up dynamically from here.
"""

__version__ = "0.1.0"
